{% extends 'dashbase.html.twig' %}

{% block content %}
    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">

                        <div class="card-body">
                            <div class="card-header card-header-primary">
                                <h4 class="card-title ">Add a new Repairer</h4>
                            </div>
                            <table class="table">

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('reparateur_index') }}">Back to the list</a>
        </li>
    </ul>
                            </div>
                    </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
